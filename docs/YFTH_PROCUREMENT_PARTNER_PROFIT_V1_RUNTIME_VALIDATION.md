# 门店采购与合伙人收益 V1 运行验证

## 验证环境

- PHP 7.4.33
- MySQL Community 8.0.46
- 数据库：独立 validation 数据库，表前缀 `eb_`
- Admin：Vue production build
- H5 / mp-weixin：HBuilderX 5.14 uni-app compiler + Node.js 18.20.8

未连接生产数据库，未执行真实采购付款、微信分账、短信或小程序上传。

## 已执行检查

### PHP 与契约

- 本轮新增和修改 PHP 文件语法全部通过。
- `yfth_supply_chain_contract_check.php`：65 项通过。
- `yfth_procurement_partner_profit_contract_check.php`：41 项通过。
- 多角色 shell 和请求降级契约通过。

### Migration

独立 MySQL 8.0.46 实际完成：

- run；
- rollback；
- rerun；
- duplicate run。

六张新表、两个规则字段、四个后台权限和五个关键唯一索引均在 run/rerun 后存在，在 rollback 后移除；重复执行保持幂等。

仓库存在两个更早的 migration 使用同一版本号，因此没有把会在到达本迁移前失败的无条件全历史 runner 伪装为成功。本轮 migration 使用独立生命周期脚本完成真实验证。

### 采购与收益真实流

- 采购金额快照正确冻结。
- 县/地/省/大区/平台五级链路正确冻结。
- 20%/10%/5%/3%/1% 金额逐项准确。
- 重复收货不会重复生成收益。
- 县级开店服务收益为 17600 元。
- 重复记录同一开店事实保持幂等。
- 采购退款反冲按快照金额生成且重复通知幂等。
- 平台分红池为有效采购业绩的 1%。
- 分红明细合计等于分红池，重复生成返回同一批次。
- 平台董事按有效开店业绩权重取得分红。

### 既有供应链回归

- 重复发货被拒绝。
- 重复收货被拒绝。
- 重复库存流水被唯一约束拒绝。
- 既有采购单、发货、收货和库存关键索引均存在。

### 构建

- Admin production build：通过；仅有项目既有 CSS 顺序和体积提示。
- H5 production build：通过；仅有项目既有体积提示。
- mp-weixin production compile：通过；本轮新增列表 key 兼容提示已消除，剩余为 CRMEB 既有骨架组件和分包建议。

## 未执行或未实现

- 未执行真实采购支付；采购继续沿用线下/既有供应链业务口径。
- 未执行真实收益打款、提现或微信分账。
- 未新增采购售后页面。退款反冲服务边界已验证，但需要未来采购售后业务入口传入权威退款事实。
- 未部署生产；部署结果必须在合并和发布后另行记录。
